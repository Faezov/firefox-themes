# Dark Forest Rain Bulat Theme v0.4.0

5000x200 animated dark forest rain Firefox theme with a readable dark chrome palette and no white margins.

- Uses an animated SVG header image.
- No scripts, no permissions, no tracking.

Install: `about:debugging#/runtime/this-firefox` -> Load Temporary Add-on -> `manifest.json`
