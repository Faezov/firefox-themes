# Deep Blue

A simple dark blue Firefox theme with cool cyan text, bright active tabs, and calm navy chrome.

- Color-only theme: no image assets.
- No scripts, no permissions, no tracking.

Install: `about:debugging#/runtime/this-firefox` -> Load Temporary Add-on -> `manifest.json`
