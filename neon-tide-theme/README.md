# Neon Tide

A dark neon Firefox theme with electric cyan waves over a deep navy frame.

- Uses a static PNG header image and bundled theme icons.
- No scripts, no permissions, no tracking.

Install: `about:debugging#/runtime/this-firefox` -> Load Temporary Add-on -> `manifest.json`
